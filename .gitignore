# Secrets & credentials
.env
token.json
client_secret.json

# Personal / generated data (don't commit these)
memory.json
reminders.json
todo.json
notes.json
smarthome.json
brain/data/
knowledge_vault/
knowledge_processed/
knowledge_input/*
!knowledge_input/.gitkeep
workspace/
memory_vault/
jarvis.log
jarvis.log*
response.mp3
test_audio.mp3

# Generated projects
Jarvis_Projects/
mobile_projects/
projects/

# Generated images (webcam, screenshots, admin reference photo)
static/*.jpg

# HTTPS certs
certs/

# Python
venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store

# Runtime BYOK keystore & MCP config (may hold live keys)
config/providers.json
config/mcp_servers.json

# Personal memory / runtime state
episodic_memory.json
jobs.json
transcript.json

# Guardrails live stores (personal facts — never commit)
USER.md
MEMORY.md

# Local model cache (vision/etc.) — binaries, never commit
.hf_cache/

# user-uploaded vision images (runtime artifacts)
static/upload_*
