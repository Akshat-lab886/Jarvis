# Flutter build outputs (regenerated by `flutter build apk` / CI).
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# Android / iOS runner shells — regenerated, never hand-edited here.
# CI runs `flutter create --platforms=android .` before building.
android/
ios/
linux/
macos/
windows/
web/

# Editor / OS
.idea/
*.iml
.DS_Store
